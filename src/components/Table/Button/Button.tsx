import React from 'react';
import { StyledButton } from './Button.style';

export const Button: React.FC = () => {
    return (
        <StyledButton>
            <span>Detail</span>
        </StyledButton>
    );
};
